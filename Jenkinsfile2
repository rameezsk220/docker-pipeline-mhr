pipeline {
    agent any
    tools {
        maven  'maven'
    }
    stages {
        stage ('git checkout'){
            steps {
                cleanWs()
                
            }
        }
        stage ('maven build'){
            steps {
                echo "this is  maven build"
            }
        }
    }
}
